{
  "private": true,
  "scripts": {
    "dev": "browser-sync start --server --files \"*.html, css/*.css, js/*.js\"",
    "start": "npm run dev"
  },
  "dependencies": {
    "todomvc-app-css": "^2.0.0",
    "todomvc-common": "^1.0.0",
    "vue": "^3.0.0"
  },
  "devDependencies": {
    "browser-sync": "^2.26.7"
  }
}
